$('선택자').함수();
  1) html() : 해당 선택자 안에 html코드 삽입, 가져오기
  2) text() : 해당 선택자 안에 text 삽입, 가져오기
  3) hide()
  4) show()
  5) toggle()
  6) css('속성','값') : css 적용
  7) val()
  14)이벤트 관련 함수들
     click(콜백함수)